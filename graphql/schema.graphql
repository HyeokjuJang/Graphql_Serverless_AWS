type Movie {
  year: Int!
  title: String!
  info: String!
}
type Query {
  movie(year: Int, title: String!): Movie
}
type Mutation {
  createMovie(year: Int!, title: String!, info: String!): Movie!
}
